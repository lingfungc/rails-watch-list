<div class="list-body">

  <%= image_tag 'Cinema.jpg', class: 'movie-banner' %>

  <div class="banner-headline">
    <h1>Here are the Greatest Movies of all time.</h1>
    <h3><span data-controller="typed-js"></span></h3>
  </div>

  <div class="container">

    <div class="movie-list">
      <h3>My Lists</h3>
      <%= link_to "Create a new list", new_list_path, class: "btn btn-flat" %>
    </div>

    <div class="movies">
      <% @lists.each do |list| %>
        <%= link_to list_path(list), class:"text-decoration-none" do %>
          <div class="movie-card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= asset_path('Cinema.jpg') %>);">
            <%= list.name %>
          </div>
        <% end %>
      <% end %>
    </div>

  </div>

</div>
